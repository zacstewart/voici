development:
  host: localhost
  database: voici_development

test:
  host: localhost
  database: voici_test

# set these environment variables on your prod server
production:
  uri: <%= ENV['MONGOHQ_URL'] %>
